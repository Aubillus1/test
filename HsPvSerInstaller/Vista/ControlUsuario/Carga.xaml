<UserControl x:Class="Actualizar.Vista.ControlUsuario.Carga"
             x:ClassModifier="public"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Usamos Viewbox para que escale automáticamente -->
        <Viewbox Stretch="Uniform">
            <Grid Width="120" Height="120">
                <!-- Aro incompleto (Path con hueco) -->
                <Path Stroke="#3B82F6" StrokeThickness="6"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                    <Path.Data>
                        <PathGeometry>
                            <PathFigure StartPoint="50,0" IsClosed="False">
                                <!-- Arco de 270° (quedará un hueco de 90°) -->
                                <ArcSegment Point="0,50" Size="50,50"
                                            SweepDirection="Clockwise"
                                            IsLargeArc="True"/>
                            </PathFigure>
                        </PathGeometry>
                    </Path.Data>
                    <Path.RenderTransform>
                        <RotateTransform x:Name="rt" CenterX="50" CenterY="50"/>
                    </Path.RenderTransform>
                </Path>
            </Grid>
        </Viewbox>
    </Grid>

    <!-- Animación con aceleración / desaceleración -->
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="rt"
                                                   Storyboard.TargetProperty="Angle"
                                                   RepeatBehavior="Forever">
                        <!-- Gira rápido -->
                        <EasingDoubleKeyFrame Value="180" KeyTime="0:0:0.6">
                            <EasingDoubleKeyFrame.EasingFunction>
                                <CubicEase EasingMode="EaseOut"/>
                            </EasingDoubleKeyFrame.EasingFunction>
                        </EasingDoubleKeyFrame>

                        <!-- Se ralentiza y queda quieto un instante -->
                        <DiscreteDoubleKeyFrame Value="180" KeyTime="0:0:0.9"/>

                        <!-- Completa el giro más lento -->
                        <EasingDoubleKeyFrame Value="360" KeyTime="0:0:1.5">
                            <EasingDoubleKeyFrame.EasingFunction>
                                <CubicEase EasingMode="EaseIn"/>
                            </EasingDoubleKeyFrame.EasingFunction>
                        </EasingDoubleKeyFrame>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>
