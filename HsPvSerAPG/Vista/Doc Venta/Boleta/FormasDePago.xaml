<Window x:Class="HsPvSerAPG.FormasDePago"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:local="clr-namespace:HsPvSerAPG"
        mc:Ignorable="d" ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        Title="FormasDePago" Height="300" Width="520" WindowStartupLocation="CenterScreen" Icon="/Utils/Imagenes/logo_pequeño.png" Background="{DynamicResource GradientBackground}">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Margin="2" Background="{DynamicResource BtnPagoBackground}" BorderThickness="0" Click="Click_BtnEfectivo">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="Money" Foreground="{DynamicResource BtnPagoBorder}" Width="100" Height="100" Margin="0,40" RenderTransformOrigin="0.5,0.5">
                    <fa:ImageAwesome.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="-11.389"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </fa:ImageAwesome.RenderTransform>
                </fa:ImageAwesome>
                <TextBlock Text="Efectivo" Foreground="{DynamicResource BtnPagoBorder}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="35"/>
            </StackPanel>
        </Button>
        <Button Grid.Column="1" Margin="2" Background="{DynamicResource BtnTarjetaBackground}" Click="Click_BtnDetallar" BorderThickness="0" >
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="CreditCard" Foreground="{DynamicResource BtnTarjetaBorder}" Width="110" Height="110" Margin="0,40" RenderTransformOrigin="0.5,0.5">
                    <fa:ImageAwesome.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="23.915"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </fa:ImageAwesome.RenderTransform>
                </fa:ImageAwesome>
                <TextBlock Text="Detallar Pago" Foreground="{DynamicResource BtnTarjetaBorder}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="25"/>
            </StackPanel>
        </Button>
        <Button Grid.Column="2" Margin="2" Background="{DynamicResource BtnSalirBackground}" BorderThickness="0" VerticalAlignment="Top" Click="Button_Click">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="TimesCircle" Foreground="{DynamicResource BtnSalirBorder}" Width="80" Height="80" Margin="0,10"/>
                <TextBlock Text="Salir" Foreground="{DynamicResource BtnSalirBorder}" HorizontalAlignment="Center" FontWeight="Bold" FontSize="40"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
