<Window x:Class="HsPvSerAPG.Vista.CompañiaSelección"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/" 
        xmlns:faWPF="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
        xmlns:local="clr-namespace:HsPvSerAPG.Vista"
        mc:Ignorable="d" ResizeMode="NoResize"
        Title="Selección" Height="307" Width="418" WindowStartupLocation="CenterScreen" Icon="/Utils/Imagenes/logo_pequeño.png" Background="{DynamicResource LinearBackground}">
    <Window.Resources>
        <Storyboard x:Key="Slider">
            <DoubleAnimation
                Storyboard.TargetName="SlideTransform"
                Storyboard.TargetProperty="X"
                From="0" To="-130" Duration="0:0:0.2"
                AccelerationRatio="0.2"
                DecelerationRatio="0.8" />
        </Storyboard>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" VerticalAlignment="Center" Margin="10,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" HorizontalAlignment="Center"  Text="Caja:" FontSize="25" FontWeight="Bold" FontFamily="Arial Black" Foreground="White"/>
            <ComboBox Grid.Column="1" x:Name="CBCaja" FontSize="20" BorderThickness="0" Margin="10,0">
                <ComboBox.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="{DynamicResource Sombras}" BlurRadius="10" Opacity="0.8"/>
                </ComboBox.Effect>
            </ComboBox>
        </Grid>
        <Grid Grid.Row="2" VerticalAlignment="Center" Margin="10,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" HorizontalAlignment="Center"  Text="Num. Lote:" FontSize="25" FontWeight="Bold" FontFamily="Arial Black" Foreground="White"/>
            <Border Grid.Column="1" Margin="10,0" Background="{DynamicResource TextBox}" BorderBrush="{DynamicResource BordeTextBox}" CornerRadius="5" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="{DynamicResource Sombras}" BlurRadius="10" Opacity="0.8"/>
                </Border.Effect>
                <TextBox x:Name="NumLote" FontSize="20" BorderThickness="0" Margin="10,0" Background="Transparent" KeyDown="NumLote_KeyDown"/>
            </Border>
        </Grid>
        <Grid Grid.Row="3" Margin="3" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="BtnIngresar" Background="{DynamicResource BotonAceptarBackground}" Width="175" Height="118" FontWeight="Bold" FontSize="20" Click="BtnIngresar_Click" BorderThickness="0">
                <Button.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="{DynamicResource BtnAceptarSombra}" BlurRadius="10" Opacity="0.8"/>
                </Button.Effect>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <faWPF:ImageAwesome Foreground="White" Icon="SignIn" Width="58" Height="58" HorizontalAlignment="Center"/>
                    <TextBlock Foreground="White" Text="Ingresar" TextAlignment="Center" FontSize="30"/>
                </StackPanel>
            </Button>
            <Button x:Name="BtnCerrar" Grid.Column="1" Background="{DynamicResource BtnCerrarBackground}" Width="175" Height="118" Margin="10,0,0,0" FontWeight="Bold" FontSize="20" BorderThickness="0" Click="Click_BtnSalir">
                <Button.Effect>
                    <DropShadowEffect ShadowDepth="0" Color="{DynamicResource SombraSalir}" BlurRadius="10" Opacity="0.8"/>
                </Button.Effect>
                <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                    <faWPF:ImageAwesome Foreground="White" Icon="SignOut" Width="52" Height="52" HorizontalAlignment="Center" Margin="0,5,0,3"/>
                    <TextBlock Foreground="White" Text="Cerrar" TextAlignment="Center" FontSize="30"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</Window>
