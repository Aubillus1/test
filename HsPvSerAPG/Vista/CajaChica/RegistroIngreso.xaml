<Window x:Class="HsPvSerAPG.Vista.CajaChica.RegistroIngreso"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HsPvSerAPG.Vista.CajaChica"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen" WindowStyle="SingleBorderWindow" ResizeMode="NoResize" Icon="/Utils/Imagenes/logo.png"
        Title="Registro de Ingreso de Caja Chica" Height="550" Width="800" Background="{DynamicResource GradientBackground}">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10">
            <ComboBox x:Name="CB" Width="180" Margin="5,0" FontSize="20" IsEnabled="False" Foreground="{DynamicResource Text2}">
                <ComboBoxItem Content="INGRESO" FontSize="30"/>
                <ComboBoxItem Content="EGRESO" FontSize="30"/>
            </ComboBox>
            <ComboBox x:Name="CMBserie" Width="100" Margin="5,0" IsEnabled="False" FontSize="20" Foreground="{DynamicResource Text2}"/>
            <Border Width="200" Background="{DynamicResource TextBoxSombreado}" Margin="5,0" CornerRadius="10">
                <TextBlock x:Name="TBNumDoc" Foreground="{DynamicResource Text}" FontSize="30" FontWeight="Bold" TextAlignment="Center" FontFamily="Bahnschrift Condensed" Background="Transparent" Margin="10,0"/>
            </Border>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="15,0,0,0">
                        <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Fecha : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                        <Border Background="{DynamicResource TextBoxSombreado}" CornerRadius="15" Width="200">
                            <TextBlock x:Name="TBFecha" FontSize="30" FontFamily="Bahnschrift" VerticalAlignment="Center" Background="Transparent" Margin="15,0"/>
                        </Border>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0">
                        <TextBlock FontSize="30" Foreground="{DynamicResource Text}" Text="Caja : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"    />
                        <Border Background="{DynamicResource TextBoxSombreado}" CornerRadius="15" Width="180">
                            <TextBlock x:Name="TBCaja" FontSize="30" FontFamily="Bahnschrift" VerticalAlignment="Center" Background="Transparent" Margin="15,0"/>
                        </Border>
                    </StackPanel>
                </Grid>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="15,5">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Usuario : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <Border Background="{DynamicResource TextBoxSombreado}" CornerRadius="15" Width="200">
                        <TextBlock x:Name="TBUsuario" Background="Transparent" Margin="15,0" FontFamily="Bahnschrift" VerticalAlignment="Center" FontSize="30" />
                    </Border>
                </StackPanel>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="15,5">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Entregado : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <Border CornerRadius="18" Background="{DynamicResource TextBox}" Width="455">
                        <TextBox x:Name="txtRAZSOC" Background="Transparent" FontSize="30" FontFamily="Bahnschrift" VerticalAlignment="Center" Margin="15,0" BorderThickness="0"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="15,5">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Motivo : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <ComboBox x:Name="CBMotivo" FontSize="30" FontFamily="Bahnschrift" Background="{DynamicResource TextBox}" Width="455" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="15,5">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Detalle : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <Border CornerRadius="15" Background="{DynamicResource TextBox}" Width="455" Height="177">
                        <TextBox x:Name="TBDetalle" FontSize="30" TextWrapping="Wrap" FontFamily="Bahnschrift" Background="Transparent" Margin="5" BorderThickness="0"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="15,5" HorizontalAlignment="Right">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Total : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <Border CornerRadius="18" Background="{DynamicResource TextBox}" Width="150">
                        <TextBox x:Name="TBMonto" Background="Transparent" FontSize="30" Margin="15,0" BorderThickness="0" VerticalContentAlignment="Center"/>
                    </Border>
                </StackPanel>
                <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="15,5" HorizontalAlignment="Left">
                    <TextBlock Width="150" Foreground="{DynamicResource Text}" FontSize="30" Text="Moneda : " TextAlignment="Right" FontFamily="Bahnschrift Condensed"/>
                    <ComboBox x:Name="CBTipMon" FontSize="30" Background="{DynamicResource TextBox}" FontFamily="Bahnschrift" VerticalAlignment="Center" Width="200" Margin="0">
                        <ComboBoxItem Content="Soles" Tag="1" IsSelected="True"/>
                        <ComboBoxItem Content="Dólares" Tag="2"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Background="{DynamicResource BtnCurrentBackground}" Margin="0,10,10,10" BorderThickness="0" Height="180" Click="Click_BtnGrabar">
                    <StackPanel VerticalAlignment="Center">
                        <fa:ImageAwesome Icon="Save" Foreground="{DynamicResource BtnCurrentBorder}" Width="80"/>
                        <TextBlock Text="Grabar" TextAlignment="Center" Foreground="{DynamicResource BtnCurrentBorder}" FontSize="40" Margin="0,10,0,0" FontFamily="Bahnschrift Condensed"/>
                    </StackPanel>
                </Button>
                <Button Grid.Row="1" Background="{DynamicResource BtnSalirBackground}" Margin="0,10,10,10" BorderThickness="0" Height="160" Click="Click_BtnSalir">
                    <StackPanel VerticalAlignment="Center">
                        <fa:ImageAwesome Icon="TimesCircle" Foreground="{DynamicResource BtnSalirBorder}" Width="80"/>
                        <TextBlock Text="Salir" TextAlignment="Center" Foreground="{DynamicResource BtnSalirBorder}" FontSize="40" Margin="0,10,0,0" FontFamily="Bahnschrift Condensed"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
    