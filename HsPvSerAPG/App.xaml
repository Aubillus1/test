<Application x:Class="HsPvSerAPG.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HsPvSerAPG"
             StartupUri="Vista/Login.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Temas/TemaClaro.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Storyboard x:Key="DesaparecerGeneral">
                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0"
                Duration="0:0:2"
                AccelerationRatio="0.2"
                DecelerationRatio="0.8"/>
            </Storyboard>


            <!-- Estilo Scrollbar -->
            <Style TargetType="ScrollBar">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="true">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.LineUpCommand" Visibility="Collapsed"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.LineDownCommand" Visibility="Collapsed"/>
                                    </Track.IncreaseRepeatButton>

                                    <Track.Thumb>
                                        <Thumb>
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <!-- Barra redondeada -->
                                                    <Border CornerRadius="6"
                                        Background="{DynamicResource Text2}"
                                        Width="10"
                                        Margin="2"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <!-- Cuerpo del ComboBox -->
                                <Border x:Name="Border" Background="{DynamicResource TextBox}" BorderBrush="{DynamicResource BordeTextBox}" BorderThickness="1" CornerRadius="5" SnapsToDevicePixels="True"/>

                                <!-- Botón desplegable -->
                                <ToggleButton Grid.Column="1" Focusable="False" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right" Width="{TemplateBinding ActualWidth}">
                                    <Grid Width="{TemplateBinding ActualWidth}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="15"/>
                                            <ColumnDefinition Width="5"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Contenido seleccionado -->
                                        <ContentPresenter Grid.Column="0" Margin="8,3,0,3" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                        <Path Grid.Column="2"
                                Data="M 0 0 L 4 4 L 8 0 Z"
                                Fill="{DynamicResource Text2}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                                    </Grid>
                                </ToggleButton>

                                <!-- Popup de la lista -->
                                <Popup 
                            Name="Popup"
                            Placement="Bottom"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True"
                            Focusable="False"
                            PopupAnimation="Slide">
                                    <Border 
                                Background="{DynamicResource TextBox}"
                                BorderBrush="{DynamicResource BordeTextBox}"
                                BorderThickness="1"
                                CornerRadius="5" Width="{TemplateBinding ActualWidth}"
                                Padding="2">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <!-- Cuando el ComboBox está deshabilitado -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.8"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--Bordear boton-->
            <Style x:Key="BordeadoButtonStyle" TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            CornerRadius="30">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <!-- No tiene efecto sin cambiar la plantilla -->
            </Style>

            <Style TargetType="Button">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="5" Opacity="0.8"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.8"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="ToggleButton">
                <Setter Property="Cursor" Value="Hand"/>
            </Style>


            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="2"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" 
                            Background="{TemplateBinding Background}" 
                            Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="LightBlue"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextBox}"/>
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Text}"/>
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
            </Style>

            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{DynamicResource DataGrid}"/>
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BordeTextBox}"/>
                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BordeTextBox}"/>
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource DataGridHeader}"/>
                <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BordeTextBox}"/>
            </Style>
            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="{DynamicResource DataGrid}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BordeTextBox}"/>
                <Setter Property="BorderThickness" Value="0,0,0,0"/>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.2"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="TextoPrecio" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="Padding" Value="5,0,5,0"/>
            </Style>

            <Style TargetType="DatePicker">
                <Setter Property="CalendarStyle" Value="{DynamicResource MiCalendarioEstilo}"/>
                <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
            </Style>
            <Style x:Key="MiCalendarioEstilo" TargetType="Calendar">
                <Setter Property="Background" Value="{DynamicResource TextBox}"/>
                <!-- Fondo calendario -->
                <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                <!-- Texto de los días -->
                <Setter Property="CalendarDayButtonStyle" Value="{DynamicResource EstiloCalendarioDia}"/>
                <Setter Property="CalendarItemStyle" Value="{DynamicResource EstiloItemCalendario}"/>
                <!--<Setter Property="CalendarButtonStyle" Value="{DynamicResource xd}"/>-->
            </Style>
            <Style x:Key="EstiloCalendarioDia" TargetType="CalendarDayButton">
                <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BordeTextBox}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Margin" Value="1"/>
                <Style.Triggers>
                    <!-- Día seleccionado -->
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="DodgerBlue"/>
                        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                    </Trigger>

                    <!-- Día de hoy -->
                    <Trigger Property="IsToday" Value="True">
                        <Setter Property="Background" Value="{DynamicResource IsTodayCalendarBackground}"/>
                        <Setter Property="Foreground" Value="{DynamicResource IsTodayCalendarText}"/>
                    </Trigger>

                    <!-- Día deshabilitado -->
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Foreground" Value="Gray"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <!-- Encabezado (Días de la semana, mes, etc.) -->
            <Style x:Key="EstiloItemCalendario" TargetType="CalendarItem">
                <Setter Property="Background" Value="DarkBlue"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>


            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource Text}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid VerticalAlignment="Center" Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Box" Width="18" Height="18" CornerRadius="4" BorderThickness="2" BorderBrush="{DynamicResource BordeTextBox}" Background="{DynamicResource TextBox}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <Path x:Name="CheckMark" Grid.Column="0" Data="M2,9 L7,14 16,4" Stroke="#FFFFFF" StrokeThickness="2.5" StrokeEndLineCap="Round" StrokeStartLineCap="Round" Visibility="Collapsed" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <ContentPresenter Grid.Column="2" VerticalAlignment="Center" RecognizesAccessKey="True" ContentSource="Content"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="#9AAFD4"/>
                                </Trigger>

                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="#4A90E2"/>
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource BordeTextBox}"/>
                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                </Trigger>

                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
